version: 1

models:
### final
  - name: aalr_pivot
    description: Assignment list report, pivoted to have one row per beneficiary per month
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_input_layer{% else %}input_layer{%- endif -%}